# /usr/local/etc/privoxy/filters/mylist
# 
# this file contains a group of user selected sites to block
# "mylist" is usually used to block sites 24/7
#
# details for the privoxy "blocked" page to display on why a site was blocked:
{ +block{site found on /usr/local/etc/privoxy/filters/mylist.} }
#
# .example.com will block all sites ending in that domain
# www.example.com will block just that site and not others in that domain
#
.example.com
